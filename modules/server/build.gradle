// :modules:server
// 服务端模块(从 reader-mt 移植)。
// Phase 0: 空占位(kotlin-jvm),确保模块可独立编译。
// Phase 2 将基于 vendor/reader-mt/build.gradle.kts(Groovy 化)填入
// Spring Boot 2.1.6 + Vert.x 3.8.1 + JavaFX + application 插件,
// 并消费 Phase 1 产出的 :legado-engine。
// :server 禁止在 io.legado.app 包下定义类(用 com.htmake.reader.*),CI 强制。
plugins {
    alias libs.plugins.kotlin.jvm
}

kotlin {
    jvmToolchain(17)
}

// Phase 2 填充:spring-boot / vertx / javafx / 依赖 :legado-engine
dependencies {
    // implementation project(':modules:legado-engine')
}